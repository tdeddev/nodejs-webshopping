<%- include('header') %>

    <form method="post" class="container mt-3">
        <div class="card">
            <div class="card-body">
                <div class="card-title">
                    เพิ่มรายการ ประเภทสินค้า
                </div>
                <div class="mt-3">
                    <label for="">ชื่อ</label>
                    <input type="text" name="name" id="" class="form-control" value="<%= groupProduct.name %>">
                </div>
                <div class="mt-3">
                    <button type="submit" class="btn btn-info">Save</button>
                </div>
            </div>
        </div>
    </form>


<%- include('footer') %>
